[project]
name = "ecmeasure"
version = "0.1.0"
description = "Echo chamber measures"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.5",
    "pandas>=2.3.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
