[project]
name = "ecmeasure"
version = "0.1.0"
description = "Echo chamber measures"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "igraph>=1.0.0",
    "networkx>=3.5",
    "pandas>=2.3.2",
    "rustworkx>=0.17.1",
]

[dependency-groups]
dev = [
    "numpy>=2.4.1",
    "pytest>=9.0.2",
]
